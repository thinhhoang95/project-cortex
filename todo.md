Like @page.tsx that uses @LeftControl1.tsx , I would like you to change @LeftControl1Regulation.tsx to remove all View Options controls that already existed in @ViewOptionsControl.tsx (including the time control, the airspace flight level range filter control), and the map view options. 

Then use the @ViewOptionsControl.tsx instead. Position the Slack View (defined in @RegulationCanvas.tsx ) on top of it. 

Make medium-size edit only. If edit fails try to edit smaller parts at once.